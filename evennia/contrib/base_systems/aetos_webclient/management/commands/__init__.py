"""
The commands themselves. One module per `evennia <name>`.

Aetos ships exactly one, `aetos`, with subcommands underneath it: Addendum B.34
asks the documentation to expose a single canonical command, so a new capability
becomes a subcommand rather than a second module beside this one.

"""
